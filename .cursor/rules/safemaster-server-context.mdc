---
description: 讨论宝塔/PM2/MySQL/自建 API 部署与真机 4xx/5xx 时，先读环境快照并避免臆测路径
alwaysApply: true
---

# 安全大师 · 服务端 / 部署上下文

当用户提到 **宝塔、PM2、safemaster-api、`/v1/`、`config.json`、MySQL、真机网络错误、扣次、404/500** 等与**生产环境**相关的问题时：

1. **优先读取** 仓库中的 `docs/SERVER_ENV.md`。若其中关键表格仍为空，不要编造路径或端口；请用户补全该文件，或粘贴 `server/verify-remote-env.sh` 在服务器上的**完整输出**。
2. **默认假设**：Cursor **不能**访问用户的宝塔或 SSH；所有检查必须写成用户可在**其服务器终端**执行的命令。
3. **排错闭环**：请用户附上 **`pm2 logs … --err`、相关 `curl` 响应体、以及 `verify-remote-env.sh` 输出** 中的具体错误，避免仅凭「报错了」猜测。
4. **勿在回复或文档中**复述用户可能泄露的密码、API Key；提醒轮换与权限最小化即可。
